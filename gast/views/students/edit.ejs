<%- include('../partials/header') %>
<table id="list">
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Phone</th>
      <th>details</th>
    </tr>
  </thead>
  <tbody>
    <tr>

        <td><%= student.name %></td>
        <td><%= student.email %></td>
        <td><%= student.phone %></td>
        <td><%= student.details ? 'Yes' : 'No' %></td>

        </tr>
      
  </tbody>
</table>

<form action="/students/<%= student._id %>" method="POST">
  <h2>Modify the student details</h2>
  <ul>
  <li><label for="name">Name:</li>
    <input id="name" type="text" name="name" value="<%= student.name %>">
  </label><br>
  <li><label>Email:</li>
    <input id="email" type="text" name="email">
  </label><br>
  <li><label for ="phone">Phone-number</li>
    <input id="phone" type="text" name="phone">
  <li><label>Details Avaliable:
      <input id="check" type="checkbox" name="Details" value=true checked>
</ul>
<button>Update student</button>
<button type="submit" formaction="/students/<%= student._id %>/delete">Delete student</button>
</form>

<%- include('../partials/footer') %>